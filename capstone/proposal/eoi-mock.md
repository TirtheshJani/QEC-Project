# Mock NRC EOI — Applied Quantum Computing Challenge (QEC stream)

> Working draft. Fill iteratively as Phases 0–5 progress. Every paragraph
> here should be backed by an artifact already in this repository. If you
> can't cite an artifact for a claim, do not put the claim here.

## Project title

*Benchmarking real-time-feasible decoders for the rotated surface code
under biased and leakage-augmented noise.* **(working — refine after Phase 5
lit review)**

## Priority area

QEC and fault tolerance — *Decoding algorithm optimization.*

## 1. Methodology

> Annex A: "well-developed methodology … key tasks and methodology … logical
> and viable workplan."

**Workplan (skeleton — fill once Phase 3 results are in):**

1. Build reproducible decoder benchmark harness (Stim → DEM → decoder →
   logical error rate). Validated against Phase 3 surface-code threshold
   reproduction.
2. Sweep the experiment matrix in `capstone/README.md` under three noise
   models.
3. Identify the latency / accuracy Pareto frontier; pick the novelty knob
   from `capstone/README.md`.
4. Implement the novelty variant; rerun the matrix; compare.
5. Write up; integrity-gate; submit.

**Reproducibility:** every reported number is generated by a script in
`capstone/experiments/` with a fixed seed and is recorded in `CHANGELOG.md`'s
accuracy table together with the commit SHA.

## 2. Project team and resources

> Annex A: roles, ability, capacity, EDI, synergies with NRC.

*(For a real EOI this is a team paragraph. For the mock, this is your
self-portfolio paragraph: enumerate phase deliverables completed, link to
notebooks, and frame the NRC researcher you'd hope to work with — likely
someone in NRC's quantum software / FT theory group.)*

## 3. Alignment with scope

> Annex A: "clearly aligns with the scope of the call for proposals."

This proposal addresses the QEC and Fault Tolerance priority area, specifically
the *Decoding algorithm optimization* objective. By producing a controlled,
reproducible head-to-head benchmark of real-time-feasible decoders under
realistic noise, the project lays the groundwork for evaluating future
decoder candidates against an open, transparent baseline — directly
supporting NRC's stated goal of "high-efficiency, noise-resistant decoders
capable of real-time signal recovery in noisy environments."

## 4. Addressing a gap

> Annex A: "value of the proposed technology and the knowledge gaps."

Filled by `capstone/proposal/literature-review.md` (PRISMA via
`/ars-lit-review --mode systematic-review`).

The gap, in one line (placeholder — rewrite after lit review): *Published
decoder benchmarks fragment across (noise model, distance, decoder family)
and rarely report latency alongside accuracy, making the real-time
feasibility question hard to settle empirically.*

## 5. Innovation

> Annex A: "sufficiently novel or innovative … advancement of the technology
> … metrics of success (IP, publications, HQP training, technology
> advancement)."

Pressure-tested by the `devils_advocate_agent` before submission. Final
novelty statement depends on which knob (a/b/c in `capstone/README.md`) is
picked at Phase 5 exit.

**Metrics of success:**

- A reproducible benchmark suite (open source, MIT) others can extend.
- A preprint on arXiv quant-ph.
- One demonstrated decoder/scheduling improvement on the chosen knob, with
  quantified accuracy and latency tradeoff.

## 6. Economic and social impacts

> Annex A: "quantitative summary of estimated benefits."

Decoder latency is on the critical path between today's lab-scale logical
qubits and fault-tolerant quantum computing at industrial scale. Every
factor of two in decoder speed at fixed accuracy translates directly into
shorter syndrome-extraction cycles and lower physical-qubit overhead at a
given logical error budget; published estimates put the implied saving for a
useful FT processor in the millions of physical qubits.

*(Fill with quantitative estimate after Phase 5 — e.g., "our preferred
variant achieves the same logical error rate as MWPM at X% of the wall-clock
cost on a representative SI1000 noise model.")*

## 7. NRC collaboration

> Annex A: "identified an NRC lead researcher … leverage NRC's resources."

*(For a real submission, name an NRC quantum-software researcher in the
Applied Quantum Computing Challenge program and describe a concrete joint
workstream. For the mock, name a plausible NRC-side lead and sketch how
they'd plug in — e.g., NRC providing access to high-qubit-count emulation
infrastructure for d≥11 sweeps, and validating decoder claims against an
NRC-internal hardware noise characterization.)*

## AI use disclosure

Produced via `/ars-disclosure --venue "NRC AQC Challenge 2026"` at the
end of the capstone. Discloses: Claude Code was used for (a) curriculum
construction, (b) code authoring under Superpowers TDD with reviewer
agents, (c) literature review under ARS integrity gates. All cited
numerical results were independently reproduced from open-source code.
